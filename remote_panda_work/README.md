# Remote PANDA Work Evidence

This directory contains public-safe experiment evidence synchronized from the local project.

Included by default: summaries, manifests, metrics, notes, diffs, and necessary stdout logs.

Excluded by policy: checkpoints, model weights, raw datasets, large feature dumps (`.npz`), archives, private credentials, and prediction tables containing sample text/image fields.
